change most asserts to regular exceptions
code overlap in html
add rainbow background option
change type from "Class" to "Rowan"
make rowan.html optional
improve layout logic to take end time into account
fix default value for empty-cell-bg-color in config files
generate sched.conf if not already present
split off parsing from try statements in convert()
make a note somewhere that TBA classes are ignored
make a note at top of schedconv.py it requires schwa-lib
add comments to rowspan cells
implement date chacking in layout()
decide on what to do about times outside 8A-12A
implement cron job on grandline
implement schedule index on grandline
add tutorials and docs to grandline
add custom css to config
add comments all over the place
add README to git
add config writer to add a Schedule to a sched.conf